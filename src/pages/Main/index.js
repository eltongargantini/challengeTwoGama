import React from 'react';

import { Container, Title } from './styles';

function Main(){
    return(
        <Container>
            <Title>Bem-vindo ao sistema de cadastro! Selecione uma opção ao lado.</Title>
        </Container>
    );
};

export default Main;